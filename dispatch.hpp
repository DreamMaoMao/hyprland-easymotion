#pragma once
#include "globals.hpp"

void easymotionExitDispatch(std::string arg);
void easymotionActionDispatch(std::string arg);
void easymotionDispatch(std::string arg);


void registerDispatchers();